apiVersion: v1
kind: Service
metadata:
  name: {{ include "mera-pehle-chart.fullname" . }}
  labels:
    {{- include "mera-pehle-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mera-pehle-chart.selectorLabels" . | nindent 4 }}
