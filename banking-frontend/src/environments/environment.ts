import { EnvironmentProviders, Provider } from '@angular/core';

export const environment = {
  production: false,
  // API Gateway URL - Single entry point for all microservices (Versioned)
  apiUrl: '/api/v1'
};
